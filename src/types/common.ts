export interface NavItem {
  label: string;
  href: string;
}

export interface TestimonialSlide {
  name: string;
  title: string;
  description: string;
  imageUrl: string;
  href: string;
  iconName: "Zap" | "Sparkles" | "Layers";
}
